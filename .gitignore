# Local Pi runtime state
.atl/
.pi/
extensions/skill-registry.ts

# Godot generated state (editor layouts, import caches, compiled textures)
# Track forced removals via `git rm -r --cached .godot/` once, then ignore entirely.
.godot/

# Python build/pycache
__pycache__/
*.pyc

# Superpowers isolated worktrees
.worktrees/
